# nasopenclaw — copy this to .env and fill in your values
# .env is gitignored and never committed

# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=123456789:AABBccDDeeFFgg...

# ── A: Anthropic / Claude ────────────────────────────────────────────────
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-...

# ── O: OpenAI / Codex ────────────────────────────────────────────────────
# No API key needed — uses OAuth from /root/.codex/auth.json
# One-time setup on NAS: codex login --device-code
# (no entry needed here)

# ── G: Google / Gemini ───────────────────────────────────────────────────
# No API key needed — Gemini is routed through CLIProxy on RO-WIN
# CLIProxy uses Gemini CLI OAuth (familyshareuniversal@gmail.com)
# Re-authenticate if Gemini stops working:
#   docker exec -it cli-proxy-api ./CLIProxyAPI -login
# GEMINI_API_KEY is no longer used.

# ── Z: Z.AI / GLM ────────────────────────────────────────────────────────
# Get from: https://api.z.ai/
ZAI_API_KEY=...

# ── ALL: Claude (Z.AI) + Gemini (CLIProxy) ───────────────────────────────
# Uses ZAI_API_KEY (above) + CLIProxy on RO-WIN (no extra key needed)
# Recommended profile for daily use — /model to switch between providers
# docker-compose --profile all up -d

# ── GitHub ────────────────────────────────────────────────────────────────
# Fine-grained PAT scoped to your org: https://github.com/settings/tokens?type=beta
# Permissions needed: Contents (read/write), Metadata (read)
GITHUB_TOKEN=github_pat_...
GITHUB_USER=your-username
GITHUB_EMAIL=your@email.com
